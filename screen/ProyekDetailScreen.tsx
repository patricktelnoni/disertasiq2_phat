import React from 'react';
import { Container, Button, Text } from 'native-base';

const ProyekDetailScreen = ({navigation}) => {
  return (
    <Container>
      <Button>
        <Text>
          Button
        </Text>
      </Button>
    </Container>
  );
};

export default ProyekDetailScreen;
